Scripts used to generate plots for the island thrush manuscript.

For some reason lines joining phylo/map don't align correctly with matplotlib>=3.2.
conda create -n thrush mkl numpy=1.20 python=3.8 cartopy=0.18 matplotlib=3.1 ete3=3.1 scikit-learn=0.24

With dependencies installed, run plots.sh.
